import { View, Text, Button, ScrollView, ImageBackground, TouchableOpacity, Animated, Alert } from "react-native"

const Settings = () => {
    return (
        <View></View>
    )
}

export default Settings