import styled from "styled-components";
import { Colors } from "../styles";

export const HorizontalLineView = styled.View`
    flex:1;
    border-color:${Colors.disable};
    margin-top:10px;
    margin-bottom:5px;
    border-width:1px;
    background-color:${Colors.disable};
`;