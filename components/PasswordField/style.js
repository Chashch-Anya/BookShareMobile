import { Pressable } from "react-native";
import { View } from "react-native";
import styled from "styled-components";

export const PasswordView = styled.View`
flex-direction: row;
align-items: center;
`;

export const VisibleView = styled.Pressable`
position: absolute;
right: 5%;
`
